export struct DeckNode {
	id: duration,
	name: string,
	level: int,
	collapsed: bool,
	new: int,
	learn: int,
	due: int,
	has_children: bool,
	parent_index: int,
}

export struct CardNode {
	question: string,
	answer: string,
}
